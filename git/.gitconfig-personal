# Personal override for ~/me/ — real identity in identity-personal (gitignored).
# Agents: ask the user for personal email and write git/identity-personal.
# install.sh symlinks identity-personal → ~/.gitconfig-identity-personal

[include]
	path = ~/.gitconfig-identity-personal

[user]
	signingkey = ~/.ssh/github-personal.pub
