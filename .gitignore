# Secrets file containing API keys and sensitive information
.zsh_secrets

# SSH private keys (NEVER commit these)
ssh/oracle
ssh/github-personal
ssh/github-work
ssh/*.key
ssh/id_*
!ssh/*.pub
!ssh/config
